'use strict';

const Node = require('./lib/node.js');
const LinkedList = module.exports = node => {
    this.head = node || null;
};

LinkedList.prototype.append(value)

LinkedList.prototype.prepend = function (val) {
    let oldHead = this.head;
    this.headd = Node ||null;
};

LinkedList.prototype.reverse()

LinkedList.prototype.remove(offset)